export default function ReplayData(props: {version: number}) {
    return (
        // version == 1 used for calls from replay.tsx
        // version == 0 used for calls from browse.tsx
        <div>data.tsx</div>
    );
}